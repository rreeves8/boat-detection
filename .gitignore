# Secrets / credentials
.env
**/.env
.cloudflare.env

# Captured login traffic (contains credentials in plaintext)
*.har

# Node / Cloudflare Workers
node_modules/
.wrangler/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Scraper output
videos/
.DS_Store

# Analysis records live in GCS now, not on disk (see counting/run.py)
counting/records.jsonl
ui/records.jsonl
infra/.terraform.lock.hcl
weights/clip/ViT-B-32.pt

# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
/counting/weights
